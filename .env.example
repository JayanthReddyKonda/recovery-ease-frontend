# ─── Frontend Environment ───────────────────────────
# VITE_API_URL: full URL to backend /api endpoint (baked into JS at build time)
# VITE_SOCKET_URL: Socket.IO server root URL (no /api path)
#
# Local dev:
#   VITE_API_URL=http://localhost:8000/api
#   VITE_SOCKET_URL=http://localhost:8000
#
# ngrok / production:
#   VITE_API_URL=https://<your-ngrok-domain>.ngrok-free.dev/api
#   VITE_SOCKET_URL=https://<your-ngrok-domain>.ngrok-free.dev
VITE_API_URL=https://your-backend-domain.example.com/api
VITE_SOCKET_URL=https://your-backend-domain.example.com
